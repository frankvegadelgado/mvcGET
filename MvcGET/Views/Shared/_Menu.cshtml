@using MvcGET.Models
@model IEnumerable<Meni>

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @Html.ActionLink("Populate", "PopulateAsync", "Data",
               new { area = "", amount = 100 },
               new
               {
                   @class = "navbar-brand",
                   onclick = "return confirm('Are you sure you want to populate the database?');",
               })

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Univerzitet <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        @foreach (var menu in Model)
                        {
                            <li>@Html.ActionLink(menu.Name, menu.ActionName, menu.ControllerName, new { area = "" }, new { @class = "navbar-brand" })</li>
                        }
                    </ul>
            </ul>
        </div>
    </div>
</div>
