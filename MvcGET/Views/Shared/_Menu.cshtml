@using MvcGET.Models
@model IEnumerable<Meni>

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">

                <li>
                    <a href="@Url.Action("PopulateAsync", "Data")" class="uifw-button xsmall black" OnClick="showProgress()"><i class="uifw-icon-standard uifw-icon-standard-check-circle xsmall "></i>Populate</a>
                </li>

                <li>
                    <a href="@Url.Action("DataWarehouse", "Data")" class="uifw-button xsmall black">Export</a>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Univerzitet <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        @foreach (var menu in Model)
                        {
                            <li>@Html.ActionLink(menu.Name, menu.ActionName, menu.ControllerName, new { area = "" }, new { @class = "navbar-brand" })</li>
                        }
                    </ul>
            </ul>
        </div>
    </div>
</div>
